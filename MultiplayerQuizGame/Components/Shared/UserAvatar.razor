@inject IUserRepository UserRepository

 
        <img class="w-20 h-20 rounded-full" src="@(BLOB_URL + AvatarFilename)" alt="Rounded avatar">


@code {


    [Parameter]
    public string AvatarFilename { get; set; }
    private const string BLOB_URL = "https://komeg1storage.blob.core.windows.net/useravatars/";



}
