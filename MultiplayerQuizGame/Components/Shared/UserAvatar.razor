@inject IUserRepository UserRepository
@inject IConfiguration Configuration
 
        <img class="w-20 h-20 rounded-full" src="@(BlobUrl + AvatarFilename)" alt="Rounded avatar">


@code {


    [Parameter]
    public string AvatarFilename { get; set; }
    private string BlobUrl => Configuration["Urls:BlobUrl"]!;



}
